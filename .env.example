# Required in production (Vercel → Project → Settings → Environment Variables).
# Any long random string. Encrypts the in-flight portal session between the
# CAPTCHA, login and OTP steps. Generate one with:  openssl rand -base64 32
PORTAL_SESSION_SECRET=
